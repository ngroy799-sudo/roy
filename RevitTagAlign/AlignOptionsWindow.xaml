<Window x:Class="RevitTagAlign.AlignOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tag Alignment Options" Height="320" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Alignment Mode:" FontWeight="Bold" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <RadioButton x:Name="rbLeft" Content="Align Left" IsChecked="True" Margin="0,2"/>
            <RadioButton x:Name="rbRight" Content="Align Right" Margin="0,2"/>
            <RadioButton x:Name="rbTop" Content="Align Top" Margin="0,2"/>
            <RadioButton x:Name="rbBottom" Content="Align Bottom" Margin="0,2"/>
            <RadioButton x:Name="rbMiddleH" Content="Align Middle (Horizontal)" Margin="0,2"/>
            <RadioButton x:Name="rbMiddleV" Content="Align Middle (Vertical)" Margin="0,2"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Leader Angle (°):" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="tbAngle" Width="60" Text="45" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0" Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
