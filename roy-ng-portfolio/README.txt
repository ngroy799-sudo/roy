# Roy NG Portfolio（單檔版）

## 點樣開（重要）

1. 解壓後只會見到一個 `index.html`（約 5MB）
2. **雙擊 `index.html`** 用 Chrome / Edge 打開
3. **唔使** 有 images folder —— 所有圖片已經內嵌喺 HTML 入面
4. 建議放去：`F:\For Cursor\roy-ng-portfolio\index.html`

如果仍然見唔到圖：用右鍵 → 用瀏覽器開啟；唔好用 Word / 記事本開。
